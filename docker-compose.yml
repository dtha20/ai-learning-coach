services:
  ai-learning-coach:
    build: .
    container_name: ai-learning-coach
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["schedule"]
